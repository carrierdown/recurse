enum Entity {
    CONTAINER,
    RM,
    VALUE,
    INTERVAL,
    PITCH_OFFSET,
    NOTE,
    RAW_NOTE,
    NESTED,
    FILL,
    NS,
    ALT,
    ALT_SHORTHAND,
    PITCH,
    TRANSPOSE,
    EVEN,
    ODD,
    FIRST,
    LAST,
    SELECT,
    ENDSELECT,
    REPEAT,
    REPEAT_SHORTHAND,
    ROOT,
    CHAIN,
    RND,
    EMPTY,
    LOOP_FACTOR,
    SELECT_INDEX,
    EVENT_RESOLUTION,
    REST,
    REST_SHORTHAND,
    INTERPOLATE,
    INVALID,
    PATTERN_LENGTH,
    SET_SCALE
}

export default Entity;